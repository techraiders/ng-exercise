(function() {
  var app = angular.module('app', []);

  /*function AccordianController() {
    var vm = this;

    // add panel
    vm.addPanel = function() {
      // code to add panel
    };

    vm.selectPanel = function() {
      // code to select panel
    };
  }

  // register the accordian component
  app.component('accordian', {
    template: '<!-- accordian-template -->',
    controller: AccordianController
  });

  function AccordianPanelController() {
    // use parents methods here
    var vm = this;

    // add panel
    vm.parent.addPanel(vm);

    // select panel
    vm.parent.selectPanel(vm);
  }

  // register the accordian-panel component
  app.component('accordianPanel', {
    // require the parent component
    // In this case parent is an instance of accordian component
    require: {
      'parent' : '^accordian',
      template: '<!-- accordian-panel-template -->',
      controller: AccordianController
    }
  });*/

})();
